import { Dimensions } from "react-native"

export const sch = Dimensions.get("window").height
export const scw = Dimensions.get("window").width
export const MenuOptions = ["Home", "About", "Contact", "Profile", "Settings"]
